# ============================================
# AlphaBot v6.1 — Environment Configuration
# ============================================

# YOUR BRAIN CREDENTIALS (personal — each team member uses their own)
BRAIN_USERNAME=your_email@example.com
BRAIN_PASSWORD=your_brain_password

# STORAGE (shared team database — DO NOT CHANGE)
STORAGE_BACKEND=supabase
SUPABASE_URL=https://pofdjlhbrtyijjbnztzf.supabase.co
SUPABASE_ANON_KEY=PASTE_ANON_KEY_HERE

# LLM API KEYS (at least one required for LLM generation)
# v6.2.1: MULTIPLE KEYS — comma-separated, auto-rotates on rate limit
# Create 5 Google AI Studio projects → 5 keys → 5× rate limit (1250 RPD)
# https://aistudio.google.com/apikey
GEMINI_API_KEYS=key1,key2,key3,key4,key5
# Or single key (legacy — still works):
# GEMINI_API_KEY=your_single_key_here

# Groq (optional fallback): https://console.groq.com/keys (free)
# Also supports multiple: GROQ_API_KEYS=key1,key2
GROQ_API_KEY=
